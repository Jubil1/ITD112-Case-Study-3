// navBar.tsx - EMPTY VERSION (No height, no space)
import { forwardRef } from "react";

const NavBar = forwardRef<HTMLElement>((_props, _ref) => {
  return null; // Render nothing at all
});

NavBar.displayName = "NavBar";

export default NavBar;